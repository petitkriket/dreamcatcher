<%= form_with(model: classfield_image, local: true) do |form| %>
  <% if classfield_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classfield_image.errors.count, "error") %> prohibited this classfield_image from being saved:</h2>

      <ul>
      <% classfield_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :classfield %>
    <%= form.text_field :classfield %>
  </div>

  <div class="field">
    <%= form.label :url %>
    <%= form.text_field :url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
